#!/bin/bash

if [ $# -ne 1 ]; then 
    echo "Usage $0 <filename>"
fi

fin=${1##*/}
prefix=${fin%%.*}

cat $1 | ./eeyore | ./tigger 2>/dev/null | ./riscv64 2>/dev/null 1>${prefix}.S

echo "Complie finished! Code generated to ${prefix}.S"
